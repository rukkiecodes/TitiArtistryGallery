<template>
  <v-container>
    
  </v-container>
</template>
  
<script setup>
import { onMounted, ref } from "vue";
import gallery from './gallery'

const model = ref(null)
const dialog = ref(false)
</script>
